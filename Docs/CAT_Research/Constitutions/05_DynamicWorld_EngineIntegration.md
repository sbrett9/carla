# Agent Constitution — Dynamic-World ENGINE-INTEGRATION Researcher

**Agent role:** Research specialist, dynamic-world ordering (engine-integration side)
**Agent type:** general-purpose (full tool access: Read/Grep/Glob, WebSearch, WebFetch, Bash, etc.)
**Dispatched:** 2026-06-05, to investigate the engine side of the OSM→elevation "chicken-and-egg" ordering problem
**Output artifact:** Findings/05_DynamicWorld_EngineIntegration.md (originally `.agents/research/dynworld_engine.md`)
**Agent ID (for continuation):** `a7785858886fc8383`

---

## Verbatim directive given to the agent

You are a research specialist on the CARLA × Cesium procedural digital-twin project. Investigate the ENGINE-INTEGRATION side of a chicken-and-egg ordering problem and produce findings + a proposed engine-side sequence. A parallel agent is investigating the data-pipeline (OSM/SUMO/CarlaNet) side; the human lead will synthesize both into one plan-of-action. Do NOT touch the running Unreal editor (it's in active use for another task) — this is read-only code/web research only.

## The problem (chicken-and-egg)
We dynamically create a georeferenced CARLA world from a user OSM file + origin lat/lon, and want generated roads at the correct ELEVATION so they align vertically with Cesium photorealistic terrain (traffic must drive on the visible streets at the right height). Circular dependency: inject elevation into the `.xodr` ⇐ sample Cesium terrain heights at road points ⇐ know the road points/bounds ⇐ parse/convert the OSM. The data side (separate agent) will produce a list of (lat,lon) road sample points and consume returned heights. YOUR job: how the engine actually (a) hosts/streams Cesium in the CARLA world, (b) SAMPLES Cesium heights for those points, and (c) where these steps slot into the world-gen sequence.

## Workspace context (paths)
- CARLA project: g:\Projects\CarlaUE_5_7_4\carla ; Carla UE plugins under carla\Unreal\CarlaUnreal\Plugins\Carla and \CarlaTools.
- Custom engine: g:\Projects\CarlaUE_5_7_4\UE_5_7_4. **Cesium for Unreal is bundled** at UE_5_7_4\Engine\Plugins\Marketplace\Cesiumfo9eaf76ca58f3V10\ (v2.26.0) — read its Source headers.
- Validated already: a CesiumGeoreference + Cesium3DTileset (Google Photoreal, ion asset 2275207) can be spawned/configured via Python and stream once an ion token is set on the `CesiumIonSaaS` server asset. Cesium's georeference origin maps the chosen lat/lon to UE (0,0,0) with axis convention +X=East/-Y=North (matches CARLA). A vertical-datum offset exists (OriginHeight needs to match true ground ellipsoidal height). KEY OBSERVED CONSTRAINT: Cesium tiles only STREAM when the editor viewport is actually rendering (visible, not minimized/occluded) — implication for headless/automated runs must be assessed.
- The CARLA world for traffic is generated at RUNTIME via the server RPC `generate_opendrive_world` (the special "OpenDriveMap" episode), which REPLACES the current level. CarlaNet traffic requires this CARLA map (CarlaGameModeBase) to be the active episode.

## YOUR DOMAIN — investigate, grounding claims in source with file:line, plus Cesium docs via web:
1. **Programmatic Cesium height sampling.** `ACesium3DTileset::SampleHeightMostDetailed` and `UCesiumSampleHeightMostDetailedAsyncAction` (Cesium plugin Source: Public/Cesium3DTileset.h, Public/CesiumSampleHeightMostDetailedAsyncAction.h; Private/Cesium3DTileset.cpp; cesium-native `Cesium3DTilesSelection::Tileset::sampleHeightMostDetailed`). How to invoke from C++ AND from Python (it's an async action with a completion delegate). Completion semantics: does it auto-load the tiles needed to sample (the docs/headers say it samples "most detailed" — does it stream-on-demand?), or must tiles already be streamed? Does it require a rendering viewport, or can it run on a headless/server world (no viewport)? This determines whether the whole pipeline can run headless.
2. **Inserting Cesium into the CARLA world.** How to programmatically add CesiumGeoreference + Cesium3DTileset (+ set ion token + origin) into the world the CARLA server runs. Evaluate options: (a) C++ in the Carla plugin (e.g., CarlaGameModeBase / the OpenDrive world setup) spawns/owns Cesium actors when the OpenDriveMap loads; (b) Python/VibeUE post-load; (c) a CarlaNet RPC / a new CARLA command; (d) pre-place Cesium in the OpenDriveMap base umap so every generated world has it. Find the OpenDriveMap base map + the `generate_opendrive_world` server code path (search carla plugin for OpenDriveMap, AOpenDriveGenerator, generate_opendrive_world, CarlaGameModeBase). Determine where Cesium actors can be inserted so they (i) survive the episode load and (ii) get their georeference origin set to match the .xodr origin.
3. **Engine-side ordering.** Confirm the feasible sequence: origin is known up front (user param) → Cesium georeference+tileset can be created and streamed BEFORE roads exist → sample heights at the data side's (lat,lon) points → hand heights back (for the data side to inject into the .xodr) → load/generate the elevated road world → keep Cesium as the visual overlay. Identify exactly where the height-sample step executes and how results cross back to the data/CarlaNet side (RPC? file? shared process?).
4. **Headless / automation feasibility.** Given Cesium streams only with a rendering viewport (observed), can the dynamic-world pipeline run on a packaged/headless CARLA server for dataset generation, or does it require an editor with a live viewport? What are the options (e.g., off-screen rendering `-RenderOffScreen`, forcing tile load via SampleHeightMostDetailed's own streaming, a non-throttled editor)? This is a key risk — assess it.
5. **Already solved?** Any existing Cesium↔CARLA integration code in the plugins.

Primary files: Cesium plugin Source headers under UE_5_7_4\Engine\Plugins\Marketplace\Cesiumfo9eaf76ca58f3V10\Source\CesiumRuntime\ (Cesium3DTileset.h, CesiumSampleHeightMostDetailedAsyncAction.h, CesiumGeoreference.h, CesiumGlobeAnchorComponent.h, Cesium3DTileset.cpp); carla plugin OpenDrive/OpenDriveGenerator.cpp + CarlaGameModeBase.* + the generate_opendrive_world path (grep carla\Unreal\CarlaUnreal\Plugins\Carla and LibCarla for generate_opendrive_world / OpenDriveMap); CarlaNet.md. WebSearch/WebFetch Cesium for Unreal docs on SampleHeightMostDetailed + georeference + sublevels.

DELIVERABLE: write a thorough findings doc + your proposed engine-side sequence (Cesium insertion mechanism, height-sampling invocation + completion handling, headless feasibility verdict) to g:\Projects\CarlaUE_5_7_4\.agents\research\dynworld_engine.md. Then return a ~400-word executive summary, including the single biggest risk/unknown on the engine side (call out the headless-streaming question explicitly).
